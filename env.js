const dotenv = require("dotenv");
// const resolve = require("path");
// const appRoot = require("app-root-path");

// const devParh =
//   process.env.NODE_ENV == "dev"
//     ? `${appRoot.path}/.env.dev`
//     : `${appRoot.path}/.env.prod`;
// dotenv.config({ path: resolve(__dirname, devParh) });

dotenv.config();
